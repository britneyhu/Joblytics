import React from 'react';
import ReactDOM from 'react-dom/client';
import App_FormFillers from './App_FormFillers';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App_FormFillers />
  </React.StrictMode>
);